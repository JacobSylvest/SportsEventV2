<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sportseventv2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsEventV2">
        <activity android:name=".TopKontakt"
            android:label="@string/head_kontakt"></activity>
        <activity android:name=".TopHelp"
            android:label="@string/head_hjaelp"/>
        <activity android:name=".Profil"
            android:label="@string/head_profil"/>
        <activity android:name=".Kalender"
            android:label="@string/head_kalender"/>
        <activity android:name=".Løb"
            android:label="@string/head_loeb"/>
        <activity android:name=".UserProfile"
            android:label="@string/head_profil"/>
        <activity android:name=".SignUp"
            android:label="@string/head_registrer"/>
        <activity android:name=".Login"
            android:label="@string/head_login"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>